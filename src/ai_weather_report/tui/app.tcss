/* === Global === */

Screen {
    background: $surface;
}

/* === Main Menu === */

MainMenuScreen {
    align: center middle;
}

#ascii-title {
    text-align: center;
    color: $accent;
    width: auto;
}

#menu-options {
    width: 40;
    height: auto;
    border: round $accent;
    padding: 1 2;
    background: $surface;
}

/* === Feed List === */

FeedListScreen {
    layout: vertical;
}

#filter-input {
    dock: top;
    margin: 0 1;
}

#feed-list {
    height: 1fr;
}

#feed-hint {
    dock: bottom;
    height: 1;
    background: $primary-background-darken-1;
    color: $text-muted;
}

#feed-status {
    dock: bottom;
    height: 1;
    background: $primary-background;
    color: $text;
    padding: 0 1;
}

ArticleListItem {
    height: 3;
    padding: 0;
}

/* === Article Detail === */

ArticleDetailScreen {
    layout: vertical;
}

#article-scroll {
    height: 1fr;
    padding: 1 2;
}

#article-title {
    text-style: bold;
    width: 1fr;
    margin-bottom: 1;
}

#article-source {
    color: $text-muted;
    width: 1fr;
}

#article-summary {
    width: 1fr;
    margin: 1 0;
}

#article-tags {
    color: $accent;
    width: 1fr;
}

#article-reports {
    color: $text-muted;
    width: 1fr;
}

#article-url {
    color: $text-muted;
    width: 1fr;
}

#article-hint {
    dock: bottom;
    height: 1;
    background: $primary-background-darken-1;
    color: $text-muted;
}

/* === Reports List === */

ReportsListScreen {
    layout: vertical;
}

#reports-list {
    height: 1fr;
}

#reports-hint {
    dock: bottom;
    height: 1;
    background: $primary-background-darken-1;
    color: $text-muted;
}

#reports-status {
    dock: bottom;
    height: 1;
    background: $primary-background;
    color: $text;
    padding: 0 1;
}

ReportListItem {
    height: 2;
    padding: 0;
}

/* === Report Detail === */

ReportDetailScreen {
    layout: vertical;
}

#report-scroll {
    height: 1fr;
    padding: 1 2;
}

#report-title {
    text-style: bold;
    width: 1fr;
    margin-bottom: 1;
}

#report-info {
    color: $text-muted;
    width: 1fr;
}

#report-stories {
    width: 1fr;
    margin: 1 0;
}

#report-playback {
    dock: bottom;
    height: 1;
    background: $accent;
    color: $text;
    padding: 0 1;
}

#report-hint {
    dock: bottom;
    height: 1;
    background: $primary-background-darken-1;
    color: $text-muted;
}
